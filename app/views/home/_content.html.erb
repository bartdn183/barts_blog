<!-- Main Content -->
<div class="container">
    <div class="row">

        <div class="col-lg-8 col-lg-offset-2 col-md-10 col-md-offset-1">
          <% @blog_posts.each do |blog_post| %>
            <div class="post-preview">
                <a href="#">
                    <h2 class="post-title">
                       <%= blog_post.title %>
                    </h2>
                    <h3 class="post-subtitle">
                        <%= blog_post.entry %>
                    </h3>
                </a>
                <p class="post-meta">Posted by <b><%= blog_post.user.first_name %> <%= blog_post.user.last_name %></b> on <%= blog_post.created_at.strftime("%B %d, %Y") %></p>
            </div>
            <hr>
          <% end %>
            <!-- Pager -->
            <ul class="pager">
                <li class="next">
                    <a href="#">Older Posts &rarr;</a>
                </li>
            </ul>
        </div>
    </div>
</div>